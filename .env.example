# Optional dev convenience only.
#
# Vite inlines this into the client bundle at build time, so ANY key set here
# is publicly readable in the deployed JavaScript. Never set it on a public
# Netlify deploy — let users enter their own key in Settings instead (it is
# stored in their browser's localStorage and never leaves it).
VITE_SUNO_API_KEY=
